/* Projects.jsx — placeholder for the /projects route.
   Step 7 will replace this with the full project grid (window-chrome cards)
   and the MF Screener flagship section (toggle, ER diagram, payload inspector). */

function Projects() {
  return (
    /* main: semantic HTML landmark */
    <main>
      <h1 style={{ fontFamily: 'var(--font-display)', color: 'var(--color-text-primary)' }}>
        PROJECTS
      </h1>
    </main>
  )
}

export default Projects
