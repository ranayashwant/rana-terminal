/* Resume.jsx — placeholder for the /resume route.
   Step 10 will replace this with the terminal-boot animation
   (typed loading line, blinking cursor) and actual resume content/PDF embed. */

function Resume() {
  return (
    /* main: semantic HTML landmark */
    <main>
      <h1 style={{ fontFamily: 'var(--font-display)', color: 'var(--color-text-primary)' }}>
        RESUME
      </h1>
    </main>
  )
}

export default Resume
