/* Contact.jsx — placeholder for the /contact route.
   Step 9 will replace this with the trade-ticket contact form
   (BUY / WATCHLIST actions, real working form with chosen backend). */

function Contact() {
  return (
    /* main: semantic HTML landmark */
    <main>
      <h1 style={{ fontFamily: 'var(--font-display)', color: 'var(--color-text-primary)' }}>
        CONTACT
      </h1>
    </main>
  )
}

export default Contact
